[package]
name = "tempus"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = { version = "0.35.0", features = ["persistence"] }
serde = { version = "1", features = ["derive"] }
actually_beep = "0.1.1"
notify-rust = "4.18.0"
